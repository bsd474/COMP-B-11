package Assignments;

public class Kite {
    public static void kiteSides() {
        int longSide = 12;
        int shortSide = 10;
        int perimeter;

        perimeter = (longSide + shortSide) * 2 ;

        System.out.println("Sides: " + longSide + " " + shortSide);
        System.out.println("Perimeter: " + perimeter);
    }
}
